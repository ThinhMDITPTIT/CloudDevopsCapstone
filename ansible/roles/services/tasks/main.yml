- name: Services configuration trans
  copy:
    src: "{{ ENVIRONMENT_NAME }}-services.yaml"
    dest: $HOME

- name: Services configure creation
  command: "./bin/kubectl apply -f {{ ENVIRONMENT_NAME }}-services.yaml"
  args:
    chdir: $HOME
